<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%-meta%>

    <title>권민성 - 포트폴리오</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Custom CSS -->
    <link rel="stylesheet" href="card.css" />
    <%-style%>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
      <div class="custom-container">
        <a class="navbar-brand fw-bold" href="/">권민성 포트폴리오</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item"><a class="nav-link" href="/">홈</a></li>
            <li class="nav-item">
              <a class="nav-link" href="/project">프로젝트 보기</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/career">경력</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/award">수상 내역</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/license">자격증</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <style>
      /* 네비게이션 바의 최대 너비 제한 */
      .custom-container {
        max-width: 1200px; /* PC에서 네비게이션 바가 너무 넓지 않도록 제한 */
        width: 90%; /* 화면 크기에 따라 자연스럽게 조절 */
        margin: 0 auto; /* 가운데 정렬 */
        padding: 0 20px; /* 좌우 패딩 추가 */
      }

      /* 네비게이션 바 링크 스타일 */
      .navbar-nav .nav-link {
        font-weight: 500;
        color: #333;
        transition: color 0.3s ease-in-out;
      }

      .navbar-nav .nav-link:hover {
        color: #007bff; /* 호버 시 파란색 강조 */
      }

      .navbar-brand {
        font-size: 1.4rem;
      }

      /* 네비게이션 바를 스크롤 시 고정 */
      .sticky-top {
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
      }

      /* 모바일에서는 네비게이션 바를 100%로 확장 */
      @media (max-width: 991px) {
        .custom-container {
          width: 100%;
          padding: 0 15px;
        }
      }
    </style>
    <!-- 방문자 기록 추적 -->
    <script>
      document.addEventListener("DOMContentLoaded", function () {
        fetch("/track")
          .then((response) => response.json())
          .then((data) => console.log("방문 기록:", data))
          .catch((error) => console.error("방문 기록 오류:", error));
      });
    </script>
    <!-- Bootstrap JS 추가 -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>

  <%-body%>
</html>

<script>
  document.addEventListener("DOMContentLoaded", function () {
    const navbar = document.querySelector(".navbar");

    setTimeout(() => {
      navbar.style.top = "0";
    }, 200);

    window.addEventListener("scroll", function () {
      if (window.scrollY > 50) {
        navbar.classList.add("scrolled");
      } else {
        navbar.classList.remove("scrolled");
      }
    });
  });
</script>
